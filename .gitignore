# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Dead code files (kept in git history)
*.disabled
*.bak
*.legacy
.vercel
.env*.local

# Agent secrets
/agents/market-intel/.env

# Market intel agent local working dir (root-only — don't match api/agents/)
/agents/
/.claude/

# Supabase CLI runtime cache (regenerated by `supabase` CLI; not source-of-truth)
supabase/.temp/

# ---- Secrets: never commit ----
# Real secrets live in Vercel/Supabase project settings, not in git.
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key
.supabase_service_key

# ---- Build / tooling artifacts ----
dev-dist
coverage
.turbo
*.tsbuildinfo

# ---- Dead code: delete it, git history is the archive ----
*.bak
*.backup
*.old
